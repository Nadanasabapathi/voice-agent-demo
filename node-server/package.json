{
  "name": "server",
  "version": "1.0.0",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node dist/app.js",
    "build": "tsc",
    "deploy": "npm install && npm run build",
    "dev": "npx nodemon --watch 'src/app.ts' --exec 'ts-node ./src/app.ts'"
  },
  "author": "Saba",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@openai/agents-realtime": "^0.3.4",
    "dotenv": "^16.4.5",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
